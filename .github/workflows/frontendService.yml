name: CI for Frontend Service

on:
  push:
    branches: [main]
    paths:
      - "apps/frontend/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "turbo.json"
      - ".github/workflows/frontendService.yml"
      - ".github/actions/setup-monorepo/action.yml"
  pull_request:
    branches: [main]
    paths:
      - "apps/frontend/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "turbo.json"
      - ".github/workflows/frontendService.yml"
      - ".github/actions/setup-monorepo/action.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate & Build Frontend
    runs-on: ubuntu-latest
    steps:
      - name: ☁️ Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: 🏗️ Setup Monorepo
        uses: ./.github/actions/setup-monorepo

      - name: 🚀 Turbo Check & Build
        # We run build alongside validation. If build is cached, it's instant.
        run: pnpm turbo run check:ci check:types test build --filter=frontend

      - name: 🌐 I18n Check
        working-directory: apps/frontend
        run: |
          pnpm extract:messages
          git diff --quiet || (
            echo "❌ Lingui catalogs are out of date. Run 'pnpm extract:messages' and commit changes."
            exit 1
          )

      - name: 📊 Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: apps/frontend/coverage
