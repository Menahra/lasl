{
  "name": "@lasl/e2e-tests",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome lint config fixtures tests utils",
    "lint:fix": "biome lint --write config fixtures tests utils",
    "format": "biome lint config fixtures tests utils",
    "format:fix": "biome format --write config fixtures tests utils",
    "check:ci": "biome check config fixtures tests utils",
    "check:fix": "biome check --write config fixtures tests utils",
    "check:types": "tsc",
    "test": "doppler run -- playwright test",
    "test:ui": "doppler run -- playwright test --ui",
    "test:headed": "doppler run -- playwright test --headed",
    "test:debug": "doppler run -- playwright test --debug",
    "test:report": "doppler run -- playwright show-report"
  },
  "devDependencies": {
    "@lasl/app-contracts": "workspace:*",
    "@playwright/test": "^1.58.2",
    "dotenv": "^17.2.4",
    "http-status-codes": "^2.3.0"
  }
}