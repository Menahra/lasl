services:
  authentication-service:
    build:
      context: .
      dockerfile: apps/authentication-service/Dockerfile
    container_name: authentication_service
    image: authentication-service:latest
    environment:
      PORT: 3000
      DOPPLER_TOKEN: ${DOPPLER_TOKEN_AUTHENTICATION_SERVICE_DEV}
    entrypoint: ["doppler", "run", "--"]
    command: ["pnpm", "start"]
    depends_on:
      - authentication-service-mongodb

  authentication-service-mongodb:
    image: mongo:latest
    container_name: authentication_service_mongodb_instance
    ports:
      # Map host port for direct access (e.g., via MongoDB Compass)
      - "${DB_HOST_PORT:-27017}:27017"
    environment:
      DOPPLER_TOKEN: ${DOPPLER_TOKEN_AUTHENTICATION_SERVICE_DEV}
    volumes:
      - mongo_data:/data/db

  api-gateway:
    build:
      context: .
      dockerfile: apps/api-gateway/Dockerfile
    container_name: api_gateway
    image: api-gateway:latest
    environment:
      PORT: 3000
      DOPPLER_TOKEN: ${DOPPLER_TOKEN_API_GATEWAY_DEV}
    entrypoint: ["doppler", "run", "--"]
    command: ["pnpm", "start"]
    depends_on:
      - authentication-service
    ports:
      - "8080:3000"

volumes:
  mongo_data:
